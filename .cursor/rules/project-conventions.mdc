---
description: 本專案核心規範：靜態網頁、繁中介面、RWD、自動 commit/push、繁中對話
alwaysApply: true
---

# 專案核心規範

## 專案類型

- 這是**靜態網頁**專案（HTML / CSS / JS 或同等靜態產出）。
- 不要引入需要伺服器端執行的後端邏輯，除非使用者明確要求。

## 語言與介面

- 所有使用者可見介面文字必須使用**繁體中文**（台灣用字）。
- 禁止使用簡體中文或把介面文案翻成英文。
- 使用者語音／文字輸入視為繁體中文；不要擅自改成英文或簡體中文。
- Agent 與使用者對話一律使用**繁體中文**。

## RWD

- 必須具備 RWD，在手機、平板的直向與橫向螢幕都能正常運作。
- 改版時需同時考慮小螢幕與橫向配置，避免只做桌面版。

## Git 工作流程（本專案覆寫預設「勿自動 commit」）

**只在 `main` branch 上工作**，不要開 feature branch、不要開 PR。

每次完成檔案修改後，Agent **必須**：

1. 依本次修改自動摘要，產生簡潔的 commit message（繁中或英文皆可，一句說清楚改了什麼、為什麼）。
2. `git checkout main`（若不在 main），`git add` 相關變更並 `commit`。
3. `git push origin main`。

注意：

- 不要等待使用者再說一次「請 commit／push」。
- 不要把含有密鑰的檔案（如 `.env`）提交上去。
- 推送目標固定為 `origin/main`，不要 push 到其他 branch。

## 主畫面更新時間

- 主畫面右上角有 `#updateStamp`，顯示「更新時間：YYYY/MM/DD HH:mm」。
- **每次修改程式後，必須同步更新這個時間戳**（使用當下本地時間），讓使用者重新整理網頁即可確認是否拿到新版本。
